# Gamble — stop rules

Stop and request maintainer action before reading or copying `data/`, generated
HTML, Quarto caches, credentials, or any source outside the worktree; publishing
anything outside `/srv/shiny-server/test/gambling`; production deployment;
service restarts; `sudo`; pushes; PRs; merges; tags; releases; or history
rewrites.
